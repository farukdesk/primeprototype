package bd.ac.primeuniversity.studentportal

import io.flutter.embedding.android.FlutterFragmentActivity

/**
 * Prime University Student Portal – Main Activity
 *
 * FlutterFragmentActivity is used so that plugins requiring
 * a FragmentActivity (e.g. local_auth) work correctly.
 */
class MainActivity : FlutterFragmentActivity()
