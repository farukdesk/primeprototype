<?xml version="1.0" encoding="utf-8"?>
<!-- Letter-grade badge; the solid colour is replaced at runtime via
     backgroundTintList with the grade band colour (see ResultSetAdapter). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/grade_other_bg"/>
    <corners android:radius="8dp"/>
</shape>
